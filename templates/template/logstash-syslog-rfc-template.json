{
  "index_patterns": [
    "logstash-syslog-rfc*"
  ],
  "template": {
    "settings": {
      "refresh_interval": "{{ os_logstash_arg.index_refresh_interval }}",
      "number_of_shards": "{{ os_servers | length }}"
    }
  }
}
